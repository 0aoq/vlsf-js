#Include <Web>

[#]
    Declare<static> <test1> = 1
    Declare<> <testvar> = test1

    Global.Out(testvar)

    Reusable <test2> [a, b] = {
        Declare<static> <result> = a * b
        Global.Out(`Result: ${result}`)
    }

    test2(5, 10)
[/]